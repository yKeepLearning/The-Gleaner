# 拾遗小程序UI实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现"拾遗"微信小程序的UI界面，包含主界面、收藏库、设置三个页面，以及底部Tab导航和相关组件。

**Architecture:** 基于微信小程序原生框架，使用TypeScript和Less实现三个页面和两个自定义组件。主界面展示赃物卡片和交易输入区，收藏库以网格形式展示历史赃物，设置页面提供基础配置。

**Tech Stack:** 微信小程序、TypeScript、Less、glass-easel组件框架

---

## 文件结构

在开始实现前，先创建所有需要的目录和文件结构：

```
miniprogram/
├── pages/
│   ├── index/          # 主界面
│   │   ├── index.ts
│   │   ├── index.wxml
│   │   ├── index.less
│   │   └── index.json
│   ├── collection/     # 收藏库
│   │   ├── collection.ts
│   │   ├── collection.wxml
│   │   ├── collection.less
│   │   └── collection.json
│   └── settings/       # 设置
│       ├── settings.ts
│       ├── settings.wxml
│       ├── settings.less
│       └── settings.json
├── components/
│   ├── card/           # 卡片组件
│   │   ├── card.ts
│   │   ├── card.wxml
│   │   ├── card.less
│   │   └── card.json
│   └── input-box/      # 输入框组件
│       ├── input-box.ts
│       ├── input-box.wxml
│       ├── input-box.less
│       └── input-box.json
├── app.ts
├── app.json
├── app.less
└── utils/
    └── util.ts
```

---

### Task 1: 配置底部Tab导航

**Files:**
- Modify: `miniprogram/app.json`

- [ ] **Step 1: 更新app.json配置底部Tab导航**

```json
{
  "pages": [
    "pages/index/index",
    "pages/collection/collection",
    "pages/settings/settings"
  ],
  "window": {
    "navigationBarTextStyle": "white",
    "navigationBarTitleText": "拾遗",
    "navigationBarBackgroundColor": "#0a0a1a",
    "backgroundColor": "#0a0a1a",
    "navigationStyle": "custom"
  },
  "tabBar": {
    "color": "#888888",
    "selectedColor": "#00f0ff",
    "backgroundColor": "#0a0a1a",
    "borderStyle": "black",
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "主界面",
        "iconPath": "images/tab/home.png",
        "selectedIconPath": "images/tab/home-active.png"
      },
      {
        "pagePath": "pages/collection/collection",
        "text": "收藏库",
        "iconPath": "images/tab/collection.png",
        "selectedIconPath": "images/tab/collection-active.png"
      },
      {
        "pagePath": "pages/settings/settings",
        "text": "设置",
        "iconPath": "images/tab/settings.png",
        "selectedIconPath": "images/tab/settings-active.png"
      }
    ]
  },
  "style": "v2",
  "rendererOptions": {
    "skyline": {
      "defaultDisplayBlock": true,
      "disableABTest": true,
      "sdkVersionBegin": "3.0.0",
      "sdkVersionEnd": "15.255.255"
    }
  },
  "componentFramework": "glass-easel",
  "sitemapLocation": "sitemap.json",
  "lazyCodeLoading": "requiredComponents"
}
```

- [ ] **Step 2: 创建Tab图标目录和占位图标**

创建目录 `miniprogram/images/tab/`，并添加6个图标文件（3个普通状态，3个选中状态）：
- home.png / home-active.png
- collection.png / collection-active.png
- settings.png / settings-active.png

图标要求：
- 尺寸：81px × 81px
- 格式：PNG，透明背景
- 普通状态：白色/灰色线条
- 选中状态：霓虹青色（#00f0ff）

- [ ] **Step 3: 验证Tab配置**

在微信开发者工具中预览，确认底部Tab导航显示正常。

---

### Task 2: 配置全局样式

**Files:**
- Modify: `miniprogram/app.less`

- [ ] **Step 1: 编写全局样式**

```less
/* app.less */
page {
  background-color: #0a0a1a;
  color: #ffffff;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  font-size: 28rpx;
  line-height: 1.5;
}

/* 噪点纹理背景 */
page::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-image: url('data:image/svg+xml;base64,');
  pointer-events: none;
  z-index: 0;
}

/* 全局容器 */
.container {
  position: relative;
  z-index: 1;
  min-height: 100vh;
  padding: 0 32rpx;
  box-sizing: border-box;
}

/* 通用卡片样式 */
.card {
  background: linear-gradient(135deg, #1a1a2e, #2a2a4e);
  border-radius: 16rpx;
  border: 1rpx solid rgba(0, 240, 255, 0.3);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.3);
  overflow: hidden;
}

/* 霓虹发光效果 */
.neon-glow {
  box-shadow: 0 0 10rpx rgba(0, 240, 255, 0.3),
              0 0 20rpx rgba(0, 240, 255, 0.2),
              0 0 30rpx rgba(0, 240, 255, 0.1);
}

/* 按钮样式 */
.btn-primary {
  background: linear-gradient(135deg, #00f0ff, #0080ff);
  color: #ffffff;
  border-radius: 8rpx;
  font-size: 28rpx;
  text-align: center;
  border: none;
  &::after {
    border: none;
  }
}

/* 文字样式 */
.text-primary {
  color: #ffffff;
}

.text-secondary {
  color: #888888;
}

.text-neon {
  color: #00f0ff;
}

/* 等宽字体 */
.font-mono {
  font-family: 'Courier New', Courier, monospace;
}
```

- [ ] **Step 2: 验证全局样式**

在微信开发者工具中预览，确认背景颜色和基础样式生效。

---

### Task 3: 创建卡片组件

**Files:**
- Create: `miniprogram/components/card/card.ts`
- Create: `miniprogram/components/card/card.wxml`
- Create: `miniprogram/components/card/card.less`
- Create: `miniprogram/components/card/card.json`

- [ ] **Step 1: 创建组件配置文件**

```json
{
  "component": true,
  "usingComponents": {}
}
```

- [ ] **Step 2: 创建组件TypeScript文件**

```typescript
// card.ts
Component({
  properties: {
    // 卡片数据
    cardData: {
      type: Object,
      value: {
        id: '',
        type: 'A', // A: 电子排泄物, B: 平行世界切片, C: 稀有掉落
        content: '',
        date: '',
        status: 'unviewed' // unviewed, viewed, claimed
      }
    },
    // 卡片尺寸模式：main(主界面) | collection(收藏库)
    mode: {
      type: String,
      value: 'main'
    }
  },

  data: {
    // 动画状态
    isAnimating: false
  },

  methods: {
    // 点击卡片
    onTapCard() {
      const { cardData } = this.data;
      this.triggerEvent('tap', { cardData });
    },

    // 获取卡片类型图标
    getTypeIcon() {
      const { type } = this.data.cardData;
      switch (type) {
        case 'A': return '📝';
        case 'B': return '🌍';
        case 'C': return '✨';
        default: return '📦';
      }
    },

    // 获取卡片类型文字
    getTypeText() {
      const { type } = this.data.cardData;
      switch (type) {
        case 'A': return '电子排泄物';
        case 'B': return '平行世界切片';
        case 'C': return '稀有掉落';
        default: return '未知类型';
      }
    }
  },

  lifetimes: {
    attached() {
      // 组件初始化时，如果是未查看状态，开始呼吸动画
      if (this.data.cardData.status === 'unviewed') {
        this.setData({ isAnimating: true });
      }
    }
  }
});
```

- [ ] **Step 3: 创建组件模板文件**

```xml
<!-- card.wxml -->
<view 
  class="card {{mode === 'main' ? 'card-main' : 'card-collection'}} {{cardData.status === 'unviewed' ? 'card-unviewed' : ''}} {{isAnimating ? 'animating' : ''}}"
  bind:tap="onTapCard"
>
  <!-- 卡片内容区 -->
  <view class="card-content">
    <!-- 类型图标 -->
    <view class="card-icon">
      <text class="icon-text">{{getTypeIcon()}}</text>
    </view>
    
    <!-- 类型文字 -->
    <view class="card-type">
      <text class="type-text">{{getTypeText()}}</text>
    </view>
    
    <!-- 内容预览（收藏库模式显示） -->
    <view wx:if="{{mode === 'collection'}}" class="card-preview">
      <text class="preview-text">{{cardData.content}}</text>
    </view>
  </view>
  
  <!-- 底部信息 -->
  <view class="card-footer">
    <text class="date-text">{{cardData.date}}</text>
    
    <!-- 已收藏标记 -->
    <view wx:if="{{cardData.status === 'claimed'}}" class="claimed-badge">
      <text class="badge-text">已收藏</text>
    </view>
  </view>
  
  <!-- 发光边框效果（未查看状态） -->
  <view wx:if="{{cardData.status === 'unviewed'}}" class="glow-border"></view>
</view>
```

- [ ] **Step 4: 创建组件样式文件**

```less
/* card.less */
.card {
  position: relative;
  background: linear-gradient(135deg, #1a1a2e, #2a2a4e);
  border-radius: 16rpx;
  border: 1rpx solid rgba(0, 240, 255, 0.3);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.3);
  overflow: hidden;
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  
  &:active {
    transform: scale(0.98);
  }
}

/* 主界面卡片尺寸 */
.card-main {
  width: 300rpx;
  height: 400rpx;
}

/* 收藏库卡片尺寸 */
.card-collection {
  width: 200rpx;
  height: 260rpx;
}

/* 未查看状态发光效果 */
.card-unviewed {
  border-color: rgba(0, 240, 255, 0.6);
  box-shadow: 0 0 10rpx rgba(0, 240, 255, 0.3),
              0 0 20rpx rgba(0, 240, 255, 0.2),
              0 0 30rpx rgba(0, 240, 255, 0.1);
}

/* 呼吸动画 */
.animating {
  animation: breathe 2s ease-in-out infinite;
}

@keyframes breathe {
  0%, 100% {
    transform: scale(1);
    box-shadow: 0 0 10rpx rgba(0, 240, 255, 0.3),
                0 0 20rpx rgba(0, 240, 255, 0.2);
  }
  50% {
    transform: scale(1.02);
    box-shadow: 0 0 15rpx rgba(0, 240, 255, 0.4),
                0 0 25rpx rgba(0, 240, 255, 0.3),
                0 0 35rpx rgba(0, 240, 255, 0.2);
  }
}

/* 卡片内容区 */
.card-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 70%;
  padding: 20rpx;
  box-sizing: border-box;
}

/* 类型图标 */
.card-icon {
  margin-bottom: 16rpx;
}

.icon-text {
  font-size: 48rpx;
}

/* 类型文字 */
.card-type {
  margin-bottom: 12rpx;
}

.type-text {
  font-size: 24rpx;
  color: #888888;
}

/* 内容预览 */
.card-preview {
  width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

.preview-text {
  font-size: 20rpx;
  color: #666666;
  line-height: 1.4;
}

/* 底部信息 */
.card-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12rpx 16rpx;
  height: 30%;
  box-sizing: border-box;
}

.date-text {
  font-size: 20rpx;
  color: #666666;
  font-family: 'Courier New', Courier, monospace;
}

/* 已收藏标记 */
.claimed-badge {
  background: rgba(0, 240, 255, 0.2);
  border-radius: 4rpx;
  padding: 4rpx 8rpx;
}

.badge-text {
  font-size: 18rpx;
  color: #00f0ff;
}

/* 发光边框效果 */
.glow-border {
  position: absolute;
  top: -2rpx;
  left: -2rpx;
  right: -2rpx;
  bottom: -2rpx;
  border-radius: 18rpx;
  border: 2rpx solid rgba(0, 240, 255, 0.6);
  pointer-events: none;
  animation: glow-pulse 2s ease-in-out infinite;
}

@keyframes glow-pulse {
  0%, 100% {
    opacity: 0.6;
  }
  50% {
    opacity: 1;
  }
}
```

- [ ] **Step 5: 验证组件**

在微信开发者工具中创建测试页面，引入卡片组件并传入测试数据，确认组件显示正常。

---

### Task 4: 创建输入框组件

**Files:**
- Create: `miniprogram/components/input-box/input-box.ts`
- Create: `miniprogram/components/input-box/input-box.wxml`
- Create: `miniprogram/components/input-box/input-box.less`
- Create: `miniprogram/components/input-box/input-box.json`

- [ ] **Step 1: 创建组件配置文件**

```json
{
  "component": true,
  "usingComponents": {}
}
```

- [ ] **Step 2: 创建组件TypeScript文件**

```typescript
// input-box.ts
Component({
  properties: {
    // 占位文字
    placeholder: {
      type: String,
      value: '用真话交换...'
    },
    // 按钮文字
    buttonText: {
      type: String,
      value: '交换'
    },
    // 是否禁用
    disabled: {
      type: Boolean,
      value: false
    }
  },

  data: {
    inputValue: '',
    isFocused: false
  },

  methods: {
    // 输入事件
    onInput(e: WechatMiniprogram.Input) {
      this.setData({
        inputValue: e.detail.value
      });
      this.triggerEvent('input', { value: e.detail.value });
    },

    // 聚焦事件
    onFocus() {
      this.setData({ isFocused: true });
      this.triggerEvent('focus');
    },

    // 失焦事件
    onBlur() {
      this.setData({ isFocused: false });
      this.triggerEvent('blur');
    },

    // 提交事件
    onSubmit() {
      const { inputValue, disabled } = this.data;
      if (disabled || !inputValue.trim()) return;
      
      this.triggerEvent('submit', { value: inputValue });
      this.setData({ inputValue: '' });
    },

    // 清空输入
    clearInput() {
      this.setData({ inputValue: '' });
    }
  }
});
```

- [ ] **Step 3: 创建组件模板文件**

```xml
<!-- input-box.wxml -->
<view class="input-box {{isFocused ? 'focused' : ''}}">
  <!-- 输入框 -->
  <view class="input-wrapper">
    <input 
      class="input-field"
      placeholder="{{placeholder}}"
      placeholder-class="placeholder-text"
      value="{{inputValue}}"
      bindinput="onInput"
      bindfocus="onFocus"
      bindblur="onBlur"
      confirm-type="send"
      bindconfirm="onSubmit"
    />
  </view>
  
  <!-- 提交按钮 -->
  <view 
    class="submit-btn {{disabled || !inputValue ? 'disabled' : ''}}"
    bind:tap="onSubmit"
  >
    <text class="btn-text">{{buttonText}}</text>
  </view>
</view>
```

- [ ] **Step 4: 创建组件样式文件**

```less
/* input-box.less */
.input-box {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 16rpx 24rpx;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 12rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  transition: border-color 0.3s ease, box-shadow 0.3s ease;
}

/* 聚焦状态 */
.input-box.focused {
  border-color: rgba(0, 240, 255, 0.5);
  box-shadow: 0 0 10rpx rgba(0, 240, 255, 0.2);
}

/* 输入框容器 */
.input-wrapper {
  flex: 1;
  height: 80rpx;
  display: flex;
  align-items: center;
}

/* 输入框 */
.input-field {
  width: 100%;
  height: 100%;
  color: #ffffff;
  font-size: 28rpx;
  background: transparent;
}

/* 占位文字 */
.placeholder-text {
  color: #666666;
  font-size: 28rpx;
}

/* 提交按钮 */
.submit-btn {
  width: 120rpx;
  height: 80rpx;
  background: linear-gradient(135deg, #00f0ff, #0080ff);
  border-radius: 8rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: opacity 0.3s ease, transform 0.2s ease;
  
  &:active {
    transform: scale(0.95);
  }
}

/* 禁用状态 */
.submit-btn.disabled {
  opacity: 0.5;
  pointer-events: none;
}

/* 按钮文字 */
.btn-text {
  color: #ffffff;
  font-size: 28rpx;
  font-weight: 500;
}
```

- [ ] **Step 5: 验证组件**

在微信开发者工具中创建测试页面，引入输入框组件，测试输入、聚焦、提交等功能。

---

### Task 5: 实现主界面

**Files:**
- Modify: `miniprogram/pages/index/index.ts`
- Modify: `miniprogram/pages/index/index.wxml`
- Modify: `miniprogram/pages/index/index.less`
- Modify: `miniprogram/pages/index/index.json`

- [ ] **Step 1: 更新页面配置文件**

```json
{
  "usingComponents": {
    "navigation-bar": "../../components/navigation-bar/navigation-bar",
    "card": "../../components/card/card",
    "input-box": "../../components/input-box/input-box"
  }
}
```

- [ ] **Step 2: 更新页面TypeScript文件**

```typescript
// index.ts
const app = getApp<IAppOption>()

Component({
  data: {
    // 今日赃物数据
    todayCard: {
      id: 'item_001',
      type: 'B',
      content: '如果三年前你接受了那份远方的工作，今天的购物小票上会写着什么',
      date: '2026-06-03',
      status: 'unviewed' as const
    },
    // 交易请求文字
    tradeRequest: '喏，这是另一个你在深夜三点写下的购物清单。告诉我，你现在最想买但舍不得买的东西是什么？换。',
    // 交易请求是否显示完毕
    isTradeTextComplete: false,
    // 是否已交易
    isTraded: false
  },

  methods: {
    // 点击卡片
    onTapCard(e: WechatMiniprogram.TouchEvent) {
      const { cardData } = e.detail;
      console.log('点击卡片:', cardData);
      
      // 标记为已查看
      this.setData({
        'todayCard.status': 'viewed'
      });
    },

    // 提交交易
    onSubmitTrade(e: WechatMiniprogram.CustomEvent) {
      const { value } = e.detail;
      console.log('交易内容:', value);
      
      // 标记为已交易
      this.setData({
        isTraded: true,
        'todayCard.status': 'claimed'
      });
      
      // 显示提示
      wx.showToast({
        title: '交易完成',
        icon: 'success'
      });
    },

    // 打字机效果
    startTypewriter() {
      const fullText = this.data.tradeRequest;
      let currentIndex = 0;
      
      const timer = setInterval(() => {
        if (currentIndex <= fullText.length) {
          this.setData({
            tradeRequest: fullText.substring(0, currentIndex)
          });
          currentIndex++;
        } else {
          clearInterval(timer);
          this.setData({ isTradeTextComplete: true });
        }
      }, 50);
    }
  },

  lifetimes: {
    attached() {
      // 页面加载时启动打字机效果
      this.startTypewriter();
    }
  }
});
```

- [ ] **Step 3: 更新页面模板文件**

```xml
<!-- index.wxml -->
<view class="page-index">
  <!-- 导航栏 -->
  <navigation-bar title="拾遗" back="{{false}}" color="white" background="#0a0a1a"></navigation-bar>
  
  <!-- 主内容区 -->
  <view class="main-content">
    <!-- 赃物卡片展示区 -->
    <view class="card-area">
      <view class="card-container">
        <card 
          cardData="{{todayCard}}" 
          mode="main"
          bind:tap="onTapCard"
        ></card>
      </view>
      
      <!-- 提示文字 -->
      <view wx:if="{{todayCard.status === 'unviewed'}}" class="hint-text">
        <text class="hint-text-content">今日赃物已到达</text>
      </view>
    </view>
    
    <!-- 交易区 -->
    <view class="trade-area">
      <!-- 交易请求文字 -->
      <view class="trade-request">
        <text class="trade-request-text font-mono">{{tradeRequest}}</text>
        <text wx:if="{{!isTradeTextComplete}}" class="cursor">|</text>
      </view>
      
      <!-- 输入框 -->
      <view wx:if="{{!isTraded}}" class="input-area">
        <input-box 
          placeholder="用真话交换..."
          buttonText="交换"
          bind:submit="onSubmitTrade"
        ></input-box>
      </view>
      
      <!-- 已交易提示 -->
      <view wx:else class="traded-hint">
        <text class="traded-text">交易完成，赃物已收入收藏库</text>
      </view>
    </view>
  </view>
</view>
```

- [ ] **Step 4: 更新页面样式文件**

```less
/* index.less */
.page-index {
  min-height: 100vh;
  background: linear-gradient(180deg, #0a0a1a, #1a1a2e);
  position: relative;
}

/* 主内容区 */
.main-content {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
  padding: 0 32rpx;
  padding-top: 200rpx; /* 为导航栏留出空间 */
  padding-bottom: 120rpx; /* 为底部Tab留出空间 */
  box-sizing: border-box;
}

/* 卡片展示区 */
.card-area {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 60vh;
}

.card-container {
  margin-bottom: 24rpx;
}

/* 提示文字 */
.hint-text {
  margin-top: 16rpx;
}

.hint-text-content {
  font-size: 24rpx;
  color: #00f0ff;
  animation: blink 1.5s ease-in-out infinite;
}

@keyframes blink {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.5;
  }
}

/* 交易区 */
.trade-area {
  padding: 32rpx 0;
}

/* 交易请求文字 */
.trade-request {
  margin-bottom: 24rpx;
  padding: 24rpx;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 12rpx;
  border-left: 4rpx solid #00f0ff;
}

.trade-request-text {
  font-size: 28rpx;
  color: #ffffff;
  line-height: 1.6;
}

.cursor {
  color: #00f0ff;
  animation: blink 0.8s step-end infinite;
}

/* 输入区域 */
.input-area {
  margin-top: 24rpx;
}

/* 已交易提示 */
.traded-hint {
  padding: 24rpx;
  background: rgba(0, 240, 255, 0.1);
  border-radius: 12rpx;
  text-align: center;
}

.traded-text {
  font-size: 28rpx;
  color: #00f0ff;
}
```

- [ ] **Step 5: 验证主界面**

在微信开发者工具中预览主界面，确认：
- 卡片正确显示，有发光和呼吸动画
- 交易请求文字有打字机效果
- 输入框可以正常输入和提交
- 交易完成后显示提示

---

### Task 6: 实现收藏库页面

**Files:**
- Create: `miniprogram/pages/collection/collection.ts`
- Create: `miniprogram/pages/collection/collection.wxml`
- Create: `miniprogram/pages/collection/collection.less`
- Create: `miniprogram/pages/collection/collection.json`

- [ ] **Step 1: 创建页面配置文件**

```json
{
  "usingComponents": {
    "navigation-bar": "../../components/navigation-bar/navigation-bar",
    "card": "../../components/card/card"
  }
}
```

- [ ] **Step 2: 创建页面TypeScript文件**

```typescript
// collection.ts
Component({
  data: {
    // 收藏列表
    collectionList: [
      {
        id: 'item_001',
        type: 'A',
        content: '输入到一半被删掉的微博草稿：今天天气真好，但是我...',
        date: '2026-06-01',
        status: 'claimed'
      },
      {
        id: 'item_002',
        type: 'B',
        content: '如果三年前你接受了那份远方的工作，今天的购物小票上会写着什么',
        date: '2026-06-02',
        status: 'claimed'
      },
      {
        id: 'item_003',
        type: 'C',
        content: '平行世界的你，在23分钟前发出、又在5秒后删掉的朋友圈草稿',
        date: '2026-06-03',
        status: 'claimed'
      }
    ],
    // 是否显示详情弹窗
    showDetail: false,
    // 当前选中的卡片
    currentCard: null as any,
    // 当前交易对话记录
    currentDialogue: {
      it_said: '喏，这是另一个你在深夜三点写下的购物清单。告诉我，你现在最想买但舍不得买的东西是什么？换。',
      you_said: '一个机械键盘，一直想买但舍不得。'
    }
  },

  methods: {
    // 点击卡片
    onTapCard(e: WechatMiniprogram.TouchEvent) {
      const { cardData } = e.detail;
      this.setData({
        showDetail: true,
        currentCard: cardData
      });
    },

    // 关闭详情弹窗
    onCloseDetail() {
      this.setData({
        showDetail: false,
        currentCard: null
      });
    },

    // 阻止事件冒泡
    preventBubble() {
      // 空方法，用于阻止事件冒泡
    }
  }
});
```

- [ ] **Step 3: 创建页面模板文件**

```xml
<!-- collection.wxml -->
<view class="page-collection">
  <!-- 导航栏 -->
  <navigation-bar title="可能性博物馆" back="{{false}}" color="white" background="#0a0a1a"></navigation-bar>
  
  <!-- 主内容区 -->
  <view class="main-content">
    <!-- 卡片网格 -->
    <view wx:if="{{collectionList.length > 0}}" class="card-grid">
      <view 
        wx:for="{{collectionList}}" 
        wx:key="id"
        class="card-item"
      >
        <card 
          cardData="{{item}}" 
          mode="collection"
          bind:tap="onTapCard"
        ></card>
      </view>
    </view>
    
    <!-- 空状态 -->
    <view wx:else class="empty-state">
      <text class="empty-icon">📦</text>
      <text class="empty-text">还没有收藏任何赃物</text>
      <text class="empty-hint">每天来这里看看它带回了什么</text>
    </view>
  </view>
  
  <!-- 详情弹窗 -->
  <view wx:if="{{showDetail}}" class="detail-modal" bind:tap="onCloseDetail">
    <view class="modal-content" catch:tap="preventBubble">
      <!-- 关闭按钮 -->
      <view class="close-btn" bind:tap="onCloseDetail">
        <text class="close-icon">✕</text>
      </view>
      
      <!-- 卡片详情 -->
      <view class="detail-card">
        <view class="detail-header">
          <text class="detail-type">{{currentCard.type === 'A' ? '电子排泄物' : currentCard.type === 'B' ? '平行世界切片' : '稀有掉落'}}</text>
          <text class="detail-date">{{currentCard.date}}</text>
        </view>
        
        <view class="detail-content">
          <text class="detail-text">{{currentCard.content}}</text>
        </view>
      </view>
      
      <!-- 交易对话记录 -->
      <view class="dialogue-section">
        <text class="dialogue-title">交易对话记录</text>
        
        <view class="dialogue-item">
          <text class="dialogue-speaker">它说：</text>
          <text class="dialogue-content">{{currentDialogue.it_said}}</text>
        </view>
        
        <view class="dialogue-item">
          <text class="dialogue-speaker">你说：</text>
          <text class="dialogue-content">{{currentDialogue.you_said}}</text>
        </view>
      </view>
    </view>
  </view>
</view>
```

- [ ] **Step 4: 创建页面样式文件**

```less
/* collection.less */
.page-collection {
  min-height: 100vh;
  background: linear-gradient(180deg, #0a0a1a, #1a1a2e);
}

/* 主内容区 */
.main-content {
  padding: 0 32rpx;
  padding-top: 200rpx; /* 为导航栏留出空间 */
  padding-bottom: 120rpx; /* 为底部Tab留出空间 */
  box-sizing: border-box;
}

/* 卡片网格 */
.card-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 24rpx;
  justify-content: space-between;
}

.card-item {
  width: calc(50% - 12rpx);
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  min-height: 60vh;
}

.empty-icon {
  font-size: 80rpx;
  margin-bottom: 24rpx;
}

.empty-text {
  font-size: 32rpx;
  color: #888888;
  margin-bottom: 12rpx;
}

.empty-hint {
  font-size: 24rpx;
  color: #666666;
}

/* 详情弹窗 */
.detail-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.8);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  padding: 64rpx;
  box-sizing: border-box;
}

.modal-content {
  width: 100%;
  max-height: 80vh;
  background: linear-gradient(135deg, #1a1a2e, #2a2a4e);
  border-radius: 24rpx;
  border: 1rpx solid rgba(0, 240, 255, 0.3);
  padding: 48rpx;
  box-sizing: border-box;
  overflow-y: auto;
  position: relative;
}

/* 关闭按钮 */
.close-btn {
  position: absolute;
  top: 24rpx;
  right: 24rpx;
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.close-icon {
  font-size: 32rpx;
  color: #888888;
}

/* 卡片详情 */
.detail-card {
  margin-bottom: 48rpx;
}

.detail-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
}

.detail-type {
  font-size: 28rpx;
  color: #00f0ff;
}

.detail-date {
  font-size: 24rpx;
  color: #666666;
  font-family: 'Courier New', Courier, monospace;
}

.detail-content {
  padding: 24rpx;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 12rpx;
}

.detail-text {
  font-size: 28rpx;
  color: #ffffff;
  line-height: 1.6;
}

/* 交易对话记录 */
.dialogue-section {
  border-top: 1rpx solid rgba(255, 255, 255, 0.1);
  padding-top: 32rpx;
}

.dialogue-title {
  font-size: 28rpx;
  color: #888888;
  margin-bottom: 24rpx;
  display: block;
}

.dialogue-item {
  margin-bottom: 24rpx;
  padding: 16rpx;
  background: rgba(255, 255, 255, 0.03);
  border-radius: 8rpx;
}

.dialogue-speaker {
  font-size: 24rpx;
  color: #00f0ff;
  margin-bottom: 8rpx;
  display: block;
}

.dialogue-content {
  font-size: 28rpx;
  color: #ffffff;
  line-height: 1.5;
}
```

- [ ] **Step 5: 验证收藏库页面**

在微信开发者工具中预览收藏库页面，确认：
- 卡片网格正确显示
- 空状态正确显示（当列表为空时）
- 点击卡片弹出详情弹窗
- 详情弹窗显示完整内容和交易对话记录

---

### Task 7: 实现设置页面

**Files:**
- Create: `miniprogram/pages/settings/settings.ts`
- Create: `miniprogram/pages/settings/settings.wxml`
- Create: `miniprogram/pages/settings/settings.less`
- Create: `miniprogram/pages/settings/settings.json`

- [ ] **Step 1: 创建页面配置文件**

```json
{
  "usingComponents": {
    "navigation-bar": "../../components/navigation-bar/navigation-bar"
  }
}
```

- [ ] **Step 2: 创建页面TypeScript文件**

```typescript
// settings.ts
Component({
  data: {
    // 设置项列表
    settingsList: [
      {
        id: 'guide',
        icon: '📖',
        title: '新手引导',
        type: 'navigate'
      },
      {
        id: 'notification',
        icon: '🔔',
        title: '通知设置',
        type: 'switch',
        value: true
      },
      {
        id: 'about',
        icon: 'ℹ️',
        title: '关于我们',
        type: 'navigate'
      },
      {
        id: 'clear',
        icon: '🗑️',
        title: '清除数据',
        type: 'navigate',
        danger: true
      }
    ],
    // 是否显示关于弹窗
    showAbout: false,
    // 关于信息
    aboutInfo: {
      name: '拾遗',
      slogan: '用真话，交换另一个世界的碎片',
      version: 'v1.0.0',
      description: '一个诞生于互联网废弃缓存空间的数据生命体。每当世界上有一条微博被秒删、一封邮件未发送、一句语音被撤回、一个可能性被放弃……这些夭折的"如果"并没有消失，而是坠入数据的暗面，被它收集、拼贴，并赋予了淘气的人格。'
    }
  },

  methods: {
    // 点击设置项
    onTapSetting(e: WechatMiniprogram.TouchEvent) {
      const { id, type } = e.currentTarget.dataset;
      
      switch (id) {
        case 'guide':
          // 重新观看新手引导
          wx.showModal({
            title: '新手引导',
            content: '确定要重新观看新手引导吗？',
            success: (res) => {
              if (res.confirm) {
                // TODO: 跳转到新手引导页面
                wx.showToast({
                  title: '功能开发中',
                  icon: 'none'
                });
              }
            }
          });
          break;
          
        case 'about':
          // 显示关于弹窗
          this.setData({ showAbout: true });
          break;
          
        case 'clear':
          // 清除数据
          wx.showModal({
            title: '清除数据',
            content: '确定要清除所有本地缓存吗？此操作不可恢复。',
            confirmColor: '#ff6b6b',
            success: (res) => {
              if (res.confirm) {
                wx.clearStorageSync();
                wx.showToast({
                  title: '清除成功',
                  icon: 'success'
                });
              }
            }
          });
          break;
      }
    },

    // 切换通知设置
    onToggleNotification(e: WechatMiniprogram.TouchEvent) {
      const { id } = e.currentTarget.dataset;
      const index = this.data.settingsList.findIndex(item => item.id === id);
      
      if (index !== -1) {
        const newValue = !this.data.settingsList[index].value;
        this.setData({
          [`settingsList[${index}].value`]: newValue
        });
        
        // TODO: 保存通知设置到本地存储
        wx.showToast({
          title: newValue ? '已开启通知' : '已关闭通知',
          icon: 'none'
        });
      }
    },

    // 关闭关于弹窗
    onCloseAbout() {
      this.setData({ showAbout: false });
    },

    // 阻止事件冒泡
    preventBubble() {
      // 空方法，用于阻止事件冒泡
    }
  }
});
```

- [ ] **Step 3: 创建页面模板文件**

```xml
<!-- settings.wxml -->
<view class="page-settings">
  <!-- 导航栏 -->
  <navigation-bar title="设置" back="{{false}}" color="white" background="#0a0a1a"></navigation-bar>
  
  <!-- 主内容区 -->
  <view class="main-content">
    <!-- 设置列表 -->
    <view class="settings-list">
      <view 
        wx:for="{{settingsList}}" 
        wx:key="id"
        class="setting-item {{item.danger ? 'danger' : ''}}"
        data-id="{{item.id}}"
        data-type="{{item.type}}"
        bind:tap="{{item.type === 'navigate' ? 'onTapSetting' : 'onToggleNotification'}}"
      >
        <!-- 左侧图标和标题 -->
        <view class="setting-left">
          <text class="setting-icon">{{item.icon}}</text>
          <text class="setting-title">{{item.title}}</text>
        </view>
        
        <!-- 右侧控件 -->
        <view class="setting-right">
          <!-- 开关控件 -->
          <switch 
            wx:if="{{item.type === 'switch'}}"
            checked="{{item.value}}"
            color="#00f0ff"
            class="setting-switch"
          />
          
          <!-- 箭头 -->
          <text wx:else class="setting-arrow">›</text>
        </view>
      </view>
    </view>
  </view>
  
  <!-- 关于弹窗 -->
  <view wx:if="{{showAbout}}" class="about-modal" bind:tap="onCloseAbout">
    <view class="modal-content" catch:tap="preventBubble">
      <!-- 关闭按钮 -->
      <view class="close-btn" bind:tap="onCloseAbout">
        <text class="close-icon">✕</text>
      </view>
      
      <!-- 关于内容 -->
      <view class="about-content">
        <text class="about-name">{{aboutInfo.name}}</text>
        <text class="about-slogan">{{aboutInfo.slogan}}</text>
        <text class="about-version">{{aboutInfo.version}}</text>
        
        <view class="about-divider"></view>
        
        <text class="about-description">{{aboutInfo.description}}</text>
      </view>
    </view>
  </view>
</view>
```

- [ ] **Step 4: 创建页面样式文件**

```less
/* settings.less */
.page-settings {
  min-height: 100vh;
  background: linear-gradient(180deg, #0a0a1a, #1a1a2e);
}

/* 主内容区 */
.main-content {
  padding: 0 32rpx;
  padding-top: 200rpx; /* 为导航栏留出空间 */
  padding-bottom: 120rpx; /* 为底部Tab留出空间 */
  box-sizing: border-box;
}

/* 设置列表 */
.settings-list {
  background: rgba(255, 255, 255, 0.05);
  border-radius: 16rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  overflow: hidden;
}

/* 设置项 */
.setting-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 32rpx;
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.05);
  transition: background 0.2s ease;
  
  &:active {
    background: rgba(255, 255, 255, 0.05);
  }
  
  &:last-child {
    border-bottom: none;
  }
}

/* 危险操作 */
.setting-item.danger {
  .setting-title {
    color: #ff6b6b;
  }
}

/* 左侧 */
.setting-left {
  display: flex;
  align-items: center;
  gap: 24rpx;
}

.setting-icon {
  font-size: 36rpx;
}

.setting-title {
  font-size: 28rpx;
  color: #ffffff;
}

/* 右侧 */
.setting-right {
  display: flex;
  align-items: center;
}

.setting-arrow {
  font-size: 36rpx;
  color: #666666;
}

.setting-switch {
  transform: scale(0.8);
}

/* 关于弹窗 */
.about-modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.8);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  padding: 64rpx;
  box-sizing: border-box;
}

.modal-content {
  width: 100%;
  background: linear-gradient(135deg, #1a1a2e, #2a2a4e);
  border-radius: 24rpx;
  border: 1rpx solid rgba(0, 240, 255, 0.3);
  padding: 48rpx;
  box-sizing: border-box;
  position: relative;
}

/* 关闭按钮 */
.close-btn {
  position: absolute;
  top: 24rpx;
  right: 24rpx;
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.close-icon {
  font-size: 32rpx;
  color: #888888;
}

/* 关于内容 */
.about-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
}

.about-name {
  font-size: 48rpx;
  color: #ffffff;
  font-weight: bold;
  margin-bottom: 16rpx;
}

.about-slogan {
  font-size: 28rpx;
  color: #00f0ff;
  margin-bottom: 12rpx;
}

.about-version {
  font-size: 24rpx;
  color: #666666;
  font-family: 'Courier New', Courier, monospace;
  margin-bottom: 32rpx;
}

.about-divider {
  width: 100%;
  height: 1rpx;
  background: rgba(255, 255, 255, 0.1);
  margin-bottom: 32rpx;
}

.about-description {
  font-size: 28rpx;
  color: #888888;
  line-height: 1.6;
}
```

- [ ] **Step 5: 验证设置页面**

在微信开发者工具中预览设置页面，确认：
- 设置列表正确显示
- 点击"新手引导"弹出确认对话框
- 点击"关于我们"弹出关于弹窗
- 点击"清除数据"弹出确认对话框
- 通知开关可以正常切换

---

### Task 8: 删除旧页面并清理

**Files:**
- Delete: `miniprogram/pages/logs/` (整个目录)

- [ ] **Step 1: 删除logs页面目录**

删除 `miniprogram/pages/logs/` 目录及其所有文件。

- [ ] **Step 2: 验证应用运行**

在微信开发者工具中重新编译项目，确认：
- 底部Tab导航正常显示
- 三个页面可以正常切换
- 所有功能正常工作

---

## 执行说明

计划完成并保存到 `docs/superpowers/plans/2026-06-03-拾遗小程序UI实现计划.md`。两种执行选项：

**1. Subagent-Driven（推荐）** - 我为每个任务分派一个新的子代理，任务之间进行审查，快速迭代

**2. Inline Execution** - 在当前会话中使用executing-plans执行任务，批量执行并设置检查点

**选择哪种方式？**
